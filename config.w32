// $Id$
// vim:ft=javascript

// If your extension references something external, use ARG_WITH
// ARG_WITH("hahastrrev", "for hahastrrev support", "no");

// Otherwise, use ARG_ENABLE
// ARG_ENABLE("hahastrrev", "enable hahastrrev support", "no");

if (PHP_HAHASTRREV != "no") {
	EXTENSION("hahastrrev", "hahastrrev.c");
}

